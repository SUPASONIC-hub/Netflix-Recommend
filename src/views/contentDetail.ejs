<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" type="image/png" sizes="32x32" href="/assets/brand-32.png" />
    <link rel="icon" type="image/png" sizes="48x48" href="/assets/brand-48.png" />
    <link rel="apple-touch-icon" sizes="180x180" href="/assets/brand-180.png" />
    <link rel="manifest" href="/site.webmanifest" />
    <link rel="shortcut icon" href="/favicon.ico" />
    <link rel="mask-icon" href="/assets/netflix-wordmark.svg" color="#e50914" />
    <meta name="theme-color" content="#101418" />
    <title><%= content.title || content.name || 'Detail' %> - Detail</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Nanum+Gothic:wght@400;700&display=swap"
      rel="stylesheet"
    />
    <link href="/styles/common.css" rel="stylesheet" />
    <style>
      body {
        font-family: 'Nanum Gothic', system-ui, -apple-system, BlinkMacSystemFont,
          'Segoe UI', sans-serif;
        font-size: 14px;
        background-color: #ffffff;
        color: #212529;
      }
      .detail-body-text {
        font-size: 1.08rem;
        line-height: 1.7;
        white-space: pre-wrap;
        overflow-wrap: anywhere;
      }
      .info-box {
        border: 1px solid #cfe2ff;
        border-radius: 12px;
        padding: 16px;
        background: #f8fbff;
      }
      .info-box h4 {
        font-size: 1rem;
        margin-bottom: 10px;
        font-weight: 700;
        color: #0b5ed7;
      }
      .rating-box {
        background: #eef5ff;
        border-color: #b6d4fe;
      }
      .rating-score {
        display: flex;
        align-items: baseline;
        gap: 8px;
        font-weight: 700;
        color: #0b5ed7;
      }
      .rating-number {
        font-size: 2.3rem;
        line-height: 1;
      }
      .rating-max {
        font-size: 1.2rem;
        opacity: 0.85;
      }
      .rating-stars {
        margin-top: 8px;
        font-size: 1.6rem;
        letter-spacing: 2px;
      }
      .star {
        position: relative;
        display: inline-block;
        color: #dbeafe;
        line-height: 1;
      }
      .star::after {
        content: '\2605';
        position: absolute;
        left: 0;
        top: 0;
        width: var(--fill, 0%);
        overflow: hidden;
        color: #0b5ed7;
        white-space: nowrap;
      }
      @media (max-width: 575.98px) {
        body {
          font-size: 13px;
        }
        .detail-body-text {
          font-size: 1rem;
        }
        .rating-number {
          font-size: 2rem;
        }
      }
    </style>
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light mb-4 border-bottom">
      <div class="container-fluid">
        <a class="navbar-brand brand-art" href="/">
          <img class="brand-mascot" src="/assets/brand.png" alt="Brand mascot" />
          <img class="brand-netflix-logo" src="/assets/netflix-wordmark.svg" alt="Netflix" />
          <span class="brand-picks">PICKS</span>
        </a>
        <button
          type="button"
          class="btn btn-outline-secondary btn-sm comment-bubble-btn"
          data-bs-toggle="modal"
          data-bs-target="#commentSummaryModal"
          data-comment-summary-trigger
          aria-label="댓글 현황 열기"
        >
          <span class="bubble-icon">&#128172;</span>
          <span
            class="comment-total-count"
            data-comment-total-count
            role="status"
            aria-live="polite"
            aria-atomic="true"
          ><%= totalCommentCount || 0 %></span>
        </button>
      </div>
    </nav>

    <div class="container">
      <div class="row mb-4">
        <% const backdropSrc = content.backdropPath ? 'https://image.tmdb.org/t/p/w780' + content.backdropPath : (content.posterPath ? 'https://image.tmdb.org/t/p/w780' + content.posterPath : (content.posterUrl || '')); %>
        <% if (backdropSrc) { %>
        <div class="col-12 mb-3">
          <div class="rounded" style="background-image: url('<%= backdropSrc %>'); background-size: cover; background-position: center; padding: 20px; min-height: 220px;">
            <div class="bg-dark bg-opacity-50 text-white p-3 rounded">
              <h2 class="mb-0"><%= content.title || content.name || '' %></h2>
            </div>
          </div>
        </div>
        <% } %>

        <div class="col-md-4">
          <% const posterSrc = content.posterUrl || (content.posterPath ? 'https://image.tmdb.org/t/p/w500' + content.posterPath : ''); %>
          <% if (posterSrc) { %>
          <img
            src="<%= posterSrc %>"
            class="img-fluid rounded"
            alt="<%= content.title || content.name || '' %>"
          />
          <% } %>
        </div>

        <div class="col-md-8">
          <div class="d-flex flex-wrap align-items-center gap-2 mb-2">
            <h1 class="mb-0"><%= content.title || content.name || '' %></h1>
            <% if (isAdmin) { %>
            <a href="/admin/content/<%= content.id %>/edit" class="btn btn-sm btn-outline-secondary">Edit</a>
            <form
              method="post"
              action="/admin/content/<%= content.id %>/delete"
              onsubmit="return confirm('Delete this recommendation?');"
              class="d-inline"
            >
              <button type="submit" class="btn btn-sm btn-outline-danger">Delete</button>
            </form>
            <% } %>
          </div>

          <p class="text-muted">
            <%= content.year || content.pubDate || '' %> · <%= content.type || 'Content' %>
          </p>

          <% const ratingValue = typeof content.myRating === 'number' ? content.myRating : (parseFloat(content.myRating) || 0); %>
          <div class="info-box rating-box mb-3">
            <h4>MY RATING</h4>
            <div class="rating-score">
              <span class="rating-number"><%= ratingValue %></span>
              <span class="rating-max">/ 5</span>
            </div>
            <div class="rating-stars" aria-label="My rating <%= ratingValue %>">
              <% for (let i = 1; i <= 5; i++) { %>
              <% const fill = Math.max(0, Math.min(1, ratingValue - (i - 1))) * 100; %>
              <span class="star" style="--fill: <%= fill %>%">&#9733;</span>
              <% } %>
            </div>
          </div>

          <div class="info-box mb-4">
            <h4>MY NOTE</h4>
            <p class="detail-body-text mb-0"><%= content.myNote %></p>
          </div>

          <% if (content.overview) { %>
          <div class="info-box mb-3">
            <h4>OVERVIEW</h4>
            <p class="detail-body-text mb-0"><%= content.overview %></p>
          </div>
          <% } %>

          <ul class="list-unstyled">
            <% if (content.releaseDate || content.firstAirDate) { %>
            <li><strong>Release:</strong> <%= content.releaseDate || content.firstAirDate %></li>
            <% } %>
            <% if (typeof content.popularity === 'number') { %>
            <li><strong>Popularity:</strong> <%= content.popularity %></li>
            <% } %>
            <% if (typeof content.voteAverage === 'number') { %>
            <li><strong>TMDB Score:</strong> <%= content.voteAverage %></li>
            <% } %>
            <% if (typeof content.voteCount === 'number') { %>
            <li><strong>TMDB Votes:</strong> <%= content.voteCount %></li>
            <% } %>
            <% if (typeof content.adult === 'boolean') { %>
            <li><strong>Adult:</strong> <%= content.adult ? 'Yes' : 'No' %></li>
            <% } %>
            <% if (content.mediaType) { %>
            <li><strong>Media Type:</strong> <%= content.mediaType %></li>
            <% } %>
            <% if (Array.isArray(content.genreNames) && content.genreNames.length) { %>
            <li><strong>Genres:</strong> <%= content.genreNames.join(', ') %></li>
            <% } else if (Array.isArray(content.genreIds) && content.genreIds.length) { %>
            <li><strong>Genres:</strong> <%= content.genreIds.join(', ') %></li>
            <% } %>
          </ul>

          <% if (content.tags && content.tags.length) { %>
          <p>
            Tags:
            <% content.tags.forEach(function (t) { %>
            <span class="badge bg-info text-dark"><%= t %></span>
            <% }); %>
          </p>
          <% } %>
        </div>
      </div>

      <hr class="border-secondary" />

      <div class="mb-4">
        <h3>Comments</h3>
        <% if (!comments || comments.length === 0) { %>
        <p>No comments yet.</p>
        <% } else { %>
        <ul class="list-group">
          <% comments.forEach(function (cm) { %>
          <li class="list-group-item">
            <strong><%= cm.nickname %></strong>
            <small class="text-muted">(<%= new Date(cm.createdAt).toLocaleString('ko-KR') %>)</small>
            <p class="mb-0"><%= cm.text %></p>
          </li>
          <% }); %>
        </ul>
        <% } %>
      </div>

      <div class="mb-5">
        <h4>Leave a Comment</h4>
        <form method="post" action="/content/<%= content.id %>/comments">
          <div class="mb-3">
            <label class="form-label">Nickname</label>
            <input type="text" name="nickname" class="form-control" required />
          </div>
          <div class="mb-3">
            <label class="form-label">Comment</label>
            <textarea name="text" class="form-control" rows="3" required></textarea>
          </div>
          <button type="submit" class="btn btn-primary">Submit</button>
        </form>
      </div>
    </div>

    <%- include('partials/commentSummaryModal') %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
