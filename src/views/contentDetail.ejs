<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" type="image/png" sizes="32x32" href="/assets/brand-32.png" />
    <link rel="icon" type="image/png" sizes="48x48" href="/assets/brand-48.png" />
    <link rel="apple-touch-icon" sizes="180x180" href="/assets/brand-180.png" />
    <link rel="manifest" href="/site.webmanifest" />
    <link rel="shortcut icon" href="/favicon.ico" />
    <link rel="mask-icon" href="/assets/netflix-wordmark.svg" color="#e50914" />
    <meta name="theme-color" content="#101418" />
    <title><%= content.title || content.name || '상세' %> - 추천 상세</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Nanum+Gothic:wght@400;700&display=swap"
      rel="stylesheet"
    />
    <link href="/styles/common.css" rel="stylesheet" />
    <style>
      body {
        font-family: 'Nanum Gothic', system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI',
          sans-serif;
        font-size: 14px;
      }
      .detail-body-text {
        font-size: 1.08rem;
        line-height: 1.7;
        white-space: pre-wrap;
      }
      .info-box {
        border: 1px solid #cfe2ff;
        border-radius: 12px;
        padding: 16px;
        background: #f8fbff;
      }
    </style>
  </head>
  <body>
    <%- include('partials/siteHeader', { page: 'contentDetail', isAdmin: isAdmin }) %>

    <div class="container">
      <div class="row mb-4">
        <% const backdropSrc = content.backdropPath ? 'https://image.tmdb.org/t/p/w780' + content.backdropPath : (content.posterPath ? 'https://image.tmdb.org/t/p/w780' + content.posterPath : (content.posterUrl || '')); %>
        <% if (backdropSrc) { %>
        <div class="col-12 mb-3">
          <div
            class="rounded"
            style="background-image: url('<%= backdropSrc %>'); background-size: cover; background-position: center; padding: 20px; min-height: 220px;"
          >
            <div class="bg-dark bg-opacity-50 text-white p-3 rounded">
              <h2 class="mb-0"><%= content.title || content.name || '' %></h2>
            </div>
          </div>
        </div>
        <% } %>

        <div class="col-md-4">
          <% const posterSrc = content.posterUrl || (content.posterPath ? 'https://image.tmdb.org/t/p/w500' + content.posterPath : ''); %>
          <% if (posterSrc) { %>
          <img
            src="<%= posterSrc %>"
            class="img-fluid rounded"
            alt="<%= content.title || content.name || '' %>"
            loading="lazy"
            decoding="async"
          />
          <% } %>
        </div>

        <div class="col-md-8">
          <div class="d-flex flex-wrap align-items-center gap-2 mb-2">
            <h1 class="mb-0"><%= content.title || content.name || '' %></h1>
            <% if (isAdmin) { %>
            <a href="/admin/content/<%= content.id %>/edit" class="btn btn-sm btn-outline-secondary">수정</a>
            <form
              method="post"
              action="/admin/content/<%= content.id %>/delete"
              onsubmit="return confirm('이 추천을 삭제할까요?');"
              class="d-inline"
            >
              <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
              <button type="submit" class="btn btn-sm btn-outline-danger">삭제</button>
            </form>
            <% } %>
          </div>

          <p class="text-muted">
            <%= content.year || content.pubDate || '' %> · <%= content.type || '콘텐츠' %>
          </p>

          <div class="info-box mb-3">
            <h4>나의 감상</h4>
            <p class="detail-body-text mb-0"><%= content.myNote %></p>
          </div>

          <% if (content.overview) { %>
          <div class="info-box mb-3">
            <h4>작품 개요</h4>
            <p class="detail-body-text mb-0"><%= content.overview %></p>
          </div>
          <% } %>

          <ul class="list-unstyled">
            <li><strong>내 평점:</strong> <%= content.myRating %> / 5</li>
            <% if (content.releaseDate || content.firstAirDate) { %>
            <li><strong>공개일:</strong> <%= content.releaseDate || content.firstAirDate %></li>
            <% } %>
            <% if (typeof content.popularity === 'number') { %>
            <li><strong>인기도:</strong> <%= content.popularity %></li>
            <% } %>
            <% if (typeof content.voteAverage === 'number') { %>
            <li><strong>TMDB 평점:</strong> <%= content.voteAverage %></li>
            <% } %>
            <% if (typeof content.voteCount === 'number') { %>
            <li><strong>TMDB 투표 수:</strong> <%= content.voteCount %></li>
            <% } %>
            <% if (typeof content.adult === 'boolean') { %>
            <li><strong>성인물 여부:</strong> <%= content.adult ? '예' : '아니오' %></li>
            <% } %>
            <% if (content.mediaType) { %>
            <li><strong>미디어 타입:</strong> <%= content.mediaType %></li>
            <% } %>
            <% if (Array.isArray(content.genreNames) && content.genreNames.length) { %>
            <li><strong>장르:</strong> <%= content.genreNames.join(', ') %></li>
            <% } else if (Array.isArray(content.genreIds) && content.genreIds.length) { %>
            <li><strong>장르:</strong> <%= content.genreIds.join(', ') %></li>
            <% } %>
          </ul>

          <% if (content.tags && content.tags.length) { %>
          <p>
            태그:
            <% content.tags.forEach(function (t) { %>
            <span class="badge bg-info text-dark"><%= t %></span>
            <% }); %>
          </p>
          <% } %>
        </div>
      </div>

      <hr class="border-secondary" />

      <div class="mb-4">
        <h3>댓글</h3>
        <% if (!comments || comments.length === 0) { %>
        <p>아직 댓글이 없습니다.</p>
        <% } else { %>
        <ul class="list-group">
          <% comments.forEach(function (cm) { %>
          <li class="list-group-item">
            <strong><%= cm.nickname %></strong>
            <small class="text-muted">
              (<%= new Date(cm.createdAt).toLocaleString('ko-KR', { timeZone: 'Asia/Seoul', year: 'numeric', month: '2-digit', day: '2-digit', hour: '2-digit', minute: '2-digit', second: '2-digit', hour12: false }) %>)
            </small>
            <p class="mb-0"><%= cm.text %></p>
          </li>
          <% }); %>
        </ul>
        <% } %>
      </div>

      <div class="mb-5">
        <h4>댓글 작성</h4>
        <form method="post" action="/content/<%= content.id %>/comments">
          <div class="mb-3">
            <label class="form-label">닉네임</label>
            <input type="text" name="nickname" class="form-control" required />
          </div>
          <div class="mb-3">
            <label class="form-label">댓글</label>
            <textarea name="text" class="form-control" rows="3" required></textarea>
          </div>
          <button type="submit" class="btn btn-primary">등록</button>
        </form>
      </div>
    </div>

    <%- include('partials/commentSummaryModal') %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
